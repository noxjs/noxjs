describe('', function () {
  it("converts inches to centimeters", function () {
    expect(1).toEqual(1);
  });

  it("converts centimeters to yards", function () {
    expect(2).toEqual(3);
  });
});